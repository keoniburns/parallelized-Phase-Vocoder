y(x,t)=Asin(kx−ωt+ϕ) y value of soud wave
A = amplitude

$$
\boxed
{y = A \sin{(2 \pi(ft+ \phi))}}
$$

- A = amplitude
- $\phi$ = phase
  - where in its cycle
- $f$ = frequency (pitch)
- $t$ = time

### omp stuff

- set audio buffer, audiofile.h
